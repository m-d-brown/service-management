[project]
name = "service-management"
version = "0.1.0"
description = "A collection of tools and libraries for managing infrastructure services"
requires-python = ">=3.10"

[tool.uv]
package = false
workspace = { members = ["python/reboot-orchestrator"] }
